<% layout('layout') -%>
<div class="container mt-3">
  <%-partial('./partials/messages', { messages: flash() })%>
  <h2 class="mt-3 mb-3">Courses I teach</h2>
  <%-partial('./partials/coursesList', { data: data, role: 'professor'})%>
  <h2 class="mt-3 mb-3">Courses I enrolled in</h2>
  <%-partial('./partials/coursesList', { data: data, role: 'student' })%>
  <h2 class="mt-3 mb-3">Courses I TA</h2>
  <%-partial('./partials/coursesList', { data: data, role: 'TA' })%>
</div>
